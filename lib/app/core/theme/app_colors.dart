import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000);
  static const Color cardBackground = Color(0xFF121212);
    static const Color cardBackgroundLighter = Color(0xFF18181C);
    static const Color borderColor = Color(0xFF32AAB7);
    static const Color textGreyColor = Color(0xFFA4A4A4);
    static const Color textGreyColor2 = Color(0xFF7E7E7E);
    static const Color borderGreyColor = Color(0xFF464646);
    static const Color greenColor = Color(0xFF154124);
    static const Color waterColor = Color(0xFF48A4E5);

  static const Color primary = Color(0xFFD7FE65); // Lime Green
  static const Color secondary = Color(0xFF7C4DFF); // Purple
  static const Color accent = Color(0xFFFF4081); // Pink
  
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textTertiary = Colors.white54;
  
  static const Color divider = Colors.white10;
  
  // Mood Gradients
  static const List<Color> moodGradient = [
    Color(0xFFFFB2A6), // Peaceful (Pinkish)
    Color(0xFFFFE1A8), // Content (Yellowish)
    Color(0xFFD7FE65), // Happy (Greenish)
    Color(0xFFA6E3FF), // Calm (Blueish)
  ];
}
